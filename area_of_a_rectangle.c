#include<stdio.h>

// This program uses a pre-defined data to calculate area. 
// You gotta length and breadth within the program each time you have a new rectangle.

int main()
{
    int length = 3, breadth = 8;
    int area = length*breadth;
    printf("The area of this rectangle is %d", area);
    return 0;
} 